[project]
name = "edu-agent"
version = "0.1.0"
description = "AI-driven education PPT generation pipeline"
requires-python = ">=3.10"
dependencies = [
    "python-docx>=1.1.0",
    "python-dotenv>=1.0.0",
    "openai>=1.0.0",
    "pydantic>=2.0.0",
    "pywin32>=300.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
